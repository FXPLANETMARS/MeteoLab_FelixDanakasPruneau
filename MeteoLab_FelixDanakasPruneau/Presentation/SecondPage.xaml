<Page x:Class="MeteoLab_FelixDanakasPruneau.Presentation.SecondPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:MeteoLab_FelixDanakasPruneau.Presentation"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:uen="using:Uno.Extensions.Navigation.UI"
      xmlns:utu="using:Uno.Toolkit.UI"
      xmlns:um="using:Uno.Material"
      Background="{ThemeResource BackgroundBrush}">

    <StackPanel Spacing="20" HorizontalAlignment="Center" VerticalAlignment="Center">

        <TextBlock Text="Résumé Météo" FontSize="30" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <StackPanel Orientation="Horizontal" Spacing="10">
            <TextBlock Text="Date :" FontWeight="Bold" Width="120"/>
            <TextBlock x:Name="TexteDate" />
        </StackPanel>

        <StackPanel Orientation="Horizontal" Spacing="10">
            <TextBlock Text="Température :" FontWeight="Bold" Width="120"/>
            <TextBlock Text="{Binding Temperature}" />
            <TextBlock Text="°C" />
        </StackPanel>

        <StackPanel Orientation="Horizontal" Spacing="10">
            <TextBlock Text="Humidité :" FontWeight="Bold" Width="120"/>
            <TextBlock Text="{Binding Humidite}" />
            <TextBlock Text="%" />
        </StackPanel>

        <StackPanel Orientation="Horizontal" Spacing="10">
            <TextBlock Text="Précipitations :" FontWeight="Bold" Width="120"/>
            <TextBlock Text="{Binding Precipitations}" />
            <TextBlock Text="mm" />
        </StackPanel>

        <Button Content="Retour" Click="Retour_Click" HorizontalAlignment="Center" Margin="0,30,0,0" Width="200"/>

    </StackPanel>
</Page>
